# Platform Packages

`nifty_stratlab` is the bounded research/backtesting platform. It is deliberately
separate from live collector latency paths and user-facing API read models.

Start with `nifty_stratlab/README.md` and `../docs/nifty-stratlab/README.md`.
