Acceptance checks:
- latest trade_date resolves correctly
- NIFTY hero row exists in dashboard summary
- ticker tape returns > 0 rows
- watchlist snapshots populate for latest trade_date
- export cache writes files into EXPORT_ROOT
- export manifest row count increases
- retention removes stale export files
