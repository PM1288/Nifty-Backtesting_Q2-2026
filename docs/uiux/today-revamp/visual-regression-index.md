# Visual regression index

Baseline: `current-screenshots/`.

After screenshots: `after-screenshots/` with Market Story, Sector Matrix and Full Board quick view at 1920×1080, 1440×900, 1024×768 and 390×844. Machine measurements are in `after-screenshots/results.json`.

The after set uses a deterministic test-only `/v1/overview` fixture. No fixture values are present in production application code.
