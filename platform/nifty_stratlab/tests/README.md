# Tests

Tests are grouped by sequential release phase. Run all tests with
`scripts/nifty_stratlab_test.sh`. A later phase passing does not waive an earlier
phase gate, target-environment evidence, or review of quarantined data.
