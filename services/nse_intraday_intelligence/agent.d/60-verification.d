Verification checklist:
- /health returns ok
- raw minute tables contain data
- feature tables contain data
- market_session_summary contains current date row
- summary endpoint returns hero + state + summary table
- section endpoint returns rows and charts
- stock endpoint returns series
- watchlist endpoint returns seeded system watchlists
- export manifest grows after export calls
