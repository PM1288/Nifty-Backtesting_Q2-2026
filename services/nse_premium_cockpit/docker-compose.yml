services:
  nse_premium_cockpit:
    build: .
    environment:
      - DATA_MODE=mock
      - MOCK_EMIT_INTERVAL_SEC=1.0
      # Optional (set when serving behind a gateway/proxy)
      # - PUBLIC_WS_URL=wss://yourdomain/ws/live
      # - PUBLIC_API_BASE=https://yourdomain
    ports:
      - "8000:8000"
