# Metric

> Evidence basis: static source inspection generated 2026-08-23T11:30:04.012Z. Runtime behaviour is labelled separately. “UNVERIFIED” means the source alone cannot establish the runtime condition.

| Field | Evidence |
| --- | --- |
| Source | [neon-stock-terminal/apps/web/src/pages/WorkspacePages.tsx](/home/novius2/NIFTY50/Nifty-Backtesting_Q2-2026/neon-stock-terminal/apps/web/src/pages/WorkspacePages.tsx#L222) |
| Used by routes | `/`, `/dashboard/stocks/:symbol`, `/analytics`, `/analytics/learn`, `/analytics/simulator`, `/analytics/indicators`, `/analytics/indicators/:slug`, `/analytics/stock/:symbol`, `/strategy/oiis-live`, `/strategy/rolling-monthly/legacy`, `/paper-trading`, `/market/nifty-500`, `/futures`, `/control-plane`, `/heatmap/change`, `/heatmap/rsi`, `/heatmap/will`, `/stock/:symbol` |
| CSS modules | `./WorkspacePages.module.css` |

## Responsibilities

Read the linked implementation for props, hooks, state, child components, conditional rendering, events, API calls, and responsive branches. The structured component map preserves the direct source evidence; intent that cannot be inferred safely is **UNVERIFIED**.
